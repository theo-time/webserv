#file extension must be .conf

#global config
client_max_body_size=2097152; #no space between key and value, one conf per line

#virtual server config
server{  #no space between keyword and brace
    listen=8080;
    host=127.0.0.1;
    server_name=www.default.com;

    root=/default;  
    index=index.html;

	methods=GET,POST,DELETE;
    client_max_body_size=512000;

	location=/kapouet{
        root=/pouic;
	    methods=GET;
    	index=custom.html;
		autoindex=on;
        client_max_body_size=10240;
	}

	location=/pouetka{
		return=https://www.google.com;
	}

    location=/cgi-bin{
        root=./cgi-bin;
	    methods=GET,POST,DELETE;
        cgi_path=/bin/bash;
        cgi_ext=.sh;
        error_page:400=/custom_400.html;
    }
}
